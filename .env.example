# Copy to .env — docker compose reads it automatically.

# Required. Signs session cookies. Generate with: openssl rand -base64 32
AUTH_SECRET=

# Docker Compose database settings. Keep the password secret and URL-safe;
# `openssl rand -hex 32` is a convenient choice.
POSTGRES_USER=pgpkeygen
POSTGRES_PASSWORD=
POSTGRES_DB=pgpkeygen

# Optional. Comma-separated emails that receive the admin role when they register.
ADMIN_EMAILS=

# Optional. Leave SMTP_HOST empty to skip passphrase emails.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
